services:
  # 1. AUTH SERVİSİ VE VERİTABANI
  auth_mongo:
    image: mongo:7.0
    container_name: auth_mongo
    networks:
      - internal_network
    # Portlara sadece iç ağdan erişilebilir

  auth_service:
    build: ./auth_service
    container_name: auth_service
    environment:
      # Kendi izole veritabanına bağlanıyor
      - MONGO_URL=${AUTH_MONGO_URL:-mongodb://auth_mongo:27017/auth_db}
      - SECRET_KEY=${SECRET_KEY:-yazlab-secret-key}
    networks:
      - internal_network
    depends_on:
      - auth_mongo

  # 2. PRODUCT SERVİSİ VE VERİTABANI
  product_mongo:
    image: mongo:7.0
    container_name: product_mongo
    networks:
      - internal_network

  product_service:
    build: ./product_service
    container_name: product_service
    environment:
      - MONGO_URL=${PRODUCT_MONGO_URL:-mongodb://product_mongo:27017/product_db}
    networks:
      - internal_network
    depends_on:
      - product_mongo

  # 3. ORDER SERVİSİ VE VERİTABANI
  order_mongo:
    image: mongo:7.0
    container_name: order_mongo
    networks:
      - internal_network

  order_service:
    build: ./order_service
    container_name: order_service
    environment:
      - MONGO_URL=${ORDER_MONGO_URL:-mongodb://order_mongo:27017/order_db}
      - AUTH_SERVICE_URL=http://auth_service:8000
      - PRODUCT_SERVICE_URL=http://product_service:8000
      - PYTHONPATH=/app
    networks:
      - internal_network
    depends_on:
      - order_mongo
      - auth_service
      - product_service

  # 4. API GATEWAY - DISPATCHER
  dispatcher_mongo:
    image: mongo:7.0
    container_name: dispatcher_mongo
    networks:
      - internal_network
  dispatcher:
    build: ./dispatcher
    container_name: dispatcher
    ports:
      - "8000:8000" # Sistemin Dış Dünyaya Açılan Tek Kapısı
    environment:
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL:-http://auth_service:8000}
      - PRODUCT_SERVICE_URL=${PRODUCT_SERVICE_URL:-http://product_service:8000}
      - ORDER_SERVICE_URL=${ORDER_SERVICE_URL:-http://order_service:8000}
      - MONGO_URL=${DISPATCHER_MONGO_URL:-mongodb://dispatcher_mongo:27017/dispatcher_db}
      - SECRET_KEY=${SECRET_KEY:-yazlab-secret-key}
      - 'DISPATCHER_ACCESS_PROFILES_BOOTSTRAP={"default-authenticated":{"subject":"default-authenticated","permissions":[{"resource":"/products","methods":["GET"]},{"resource":"/orders","methods":["GET"]}]},"dispatcher-user":{"subject":"dispatcher-user","permissions":[{"resource":"/products","methods":["GET","POST","PUT","PATCH","DELETE"]},{"resource":"/orders","methods":["GET","POST","PATCH","DELETE"]}]},"alice":{"subject":"alice","permissions":[{"resource":"/products","methods":["GET","POST","PUT","PATCH","DELETE"]},{"resource":"/orders","methods":["GET","POST","PATCH","DELETE"]}]},"locust-writer":{"subject":"locust-writer","permissions":[{"resource":"/products","methods":["GET","POST","PUT","PATCH","DELETE"]},{"resource":"/orders","methods":["GET","POST","PATCH","DELETE"]}]},"bob":{"subject":"bob","permissions":[{"resource":"/products","methods":["GET"]},{"resource":"/orders","methods":["GET"]}]}}'
    networks:
      - internal_network
    depends_on:
      - auth_service
      - product_service
      - order_service

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
    ports:
      - "9090:9090"
    networks:
      - internal_network
    depends_on:
      - dispatcher
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    profiles:
      - monitoring

  grafana:
    image: grafana/grafana:11.2.0
    container_name: grafana
    ports:
      - "4000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
    networks:
      - internal_network
    depends_on:
      - prometheus
    volumes:
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    profiles:
      - monitoring

  auth_tests:
    build:
      context: ./auth_service
      target: test
    working_dir: /workspace
    environment:
      - MONGO_URL=${AUTH_MONGO_URL:-mongodb://auth_mongo:27017/auth_db}
      - SECRET_KEY=${SECRET_KEY:-yazlab-secret-key}
      - PYTHONPATH=/workspace
    networks:
      - internal_network
    depends_on:
      - auth_mongo
    volumes:
      - ./auth_service:/workspace
    profiles:
      - test
    command: ["pytest", "tests"]

  dispatcher_tests:
    build:
      context: ./dispatcher
      target: test
    working_dir: /workspace
    environment:
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL:-http://auth_service:8000}
      - PRODUCT_SERVICE_URL=${PRODUCT_SERVICE_URL:-http://product_service:8000}
      - ORDER_SERVICE_URL=${ORDER_SERVICE_URL:-http://order_service:8000}
      - MONGO_URL=${DISPATCHER_MONGO_URL:-mongodb://dispatcher_mongo:27017/dispatcher_db}
      - SECRET_KEY=${SECRET_KEY:-yazlab-secret-key}
      - 'DISPATCHER_ACCESS_PROFILES_BOOTSTRAP={"default-authenticated":{"subject":"default-authenticated","permissions":[{"resource":"/products","methods":["GET"]},{"resource":"/orders","methods":["GET"]}]},"dispatcher-user":{"subject":"dispatcher-user","permissions":[{"resource":"/products","methods":["GET","POST","PUT","PATCH","DELETE"]},{"resource":"/orders","methods":["GET","POST","PATCH","DELETE"]}]},"alice":{"subject":"alice","permissions":[{"resource":"/products","methods":["GET","POST","PUT","PATCH","DELETE"]},{"resource":"/orders","methods":["GET","POST","PATCH","DELETE"]}]},"locust-writer":{"subject":"locust-writer","permissions":[{"resource":"/products","methods":["GET","POST","PUT","PATCH","DELETE"]},{"resource":"/orders","methods":["GET","POST","PATCH","DELETE"]}]},"bob":{"subject":"bob","permissions":[{"resource":"/products","methods":["GET"]},{"resource":"/orders","methods":["GET"]}]}}'
      - PYTHONPATH=/workspace
    networks:
      - internal_network
    depends_on:
      - dispatcher_mongo
      - auth_service
      - product_service
      - order_service
    volumes:
      - ./dispatcher:/workspace
    profiles:
      - test
    command: ["pytest", "tests"]

  product_tests:
    build:
      context: ./product_service
      target: test
    working_dir: /workspace
    environment:
      - MONGO_URL=${PRODUCT_MONGO_URL:-mongodb://product_mongo:27017/product_db}
      - PYTHONPATH=/workspace
    networks:
      - internal_network
    depends_on:
      - product_mongo
    volumes:
      - ./product_service:/workspace
    profiles:
      - test
    command: ["pytest", "tests"]

  order_tests:
    build:
      context: ./order_service
      target: test
    working_dir: /workspace
    environment:
      - MONGO_URL=${ORDER_MONGO_URL:-mongodb://order_mongo:27017/order_db}
      - PYTHONPATH=/workspace
    networks:
      - internal_network
    depends_on:
      - order_mongo
    volumes:
      - ./order_service:/workspace
    profiles:
      - test
    command: ["pytest", "tests"]

networks:
  internal_network:
    driver: bridge
